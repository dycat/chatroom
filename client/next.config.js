/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, // Change this to false to make chartroom only render once with useEffect
  swcMinify: true,
}

module.exports = nextConfig
